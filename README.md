# Water Is Life — Local Dev

Run the dev server:

```bash
npm install
npm run dev
```

This scaffolds a minimal Vite + React app with Tailwind CSS and places the provided JSX component at `src/App.jsx`.
